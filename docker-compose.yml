version: '3.8'

services:
  db:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_DATABASE: Nike
      MYSQL_ROOT_PASSWORD: 02111109
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql

  server:
    build: ./server
    restart: always
    ports:
      - "5000:5000"
    environment:
      - DB_HOST=db
      - DB_USER=root
      - DB_PASSWORD=02111109
      - DB_NAME=Nike
      - PORT=5000
      - CORS_ORIGIN=http://103.216.119.220:3000
      - JWT_SECRET=44RIMb8tam27^%@3@JR
      - SALT_ROUND=10
      - EMAIL_USER=nikevnofficial@gmail.com
      - EMAIL_PASS=dglkptwlmrlwbmzp
      - SMTP_HOST=smtp.gmail.com
      - SMTP_PORT=587
      - SMTP_MAIL=nikevnofficial@gmail.com
      - SMTP_PASSWORD=dglkptwlmrlwbmzp
    depends_on:
      - db
    volumes:
      - ./server/uploads:/app/uploads

  client:
    build:
      context: ./client
      args:
        - REACT_APP_API_URL=http://103.216.119.220:5000
    restart: always
    ports:
      - "3000:80"
    depends_on:
      - server

volumes:
  db_data:
